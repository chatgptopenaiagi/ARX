# Security
Report vulnerabilities through GitHub security advisories. ARX never executes inspected packages. Diagnostic subprocesses are fixed trusted version probes with argument arrays and timeouts. Do not attach reports containing private data to public issues.

