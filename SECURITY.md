# Security
Report vulnerabilities through GitHub security advisories. ARX never executes inspected packages or project scripts. Project manifest reads are recognized-path, size-bounded, encoding-checked, and symlink-safe. Diagnostic subprocesses are fixed trusted probes with argument arrays, `shell=False`, captured output, and timeouts. The planner never applies remediation. Do not attach reports containing private data to public issues.

