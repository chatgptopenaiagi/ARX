# Software DNA
Software DNA includes path, size, SHA-256, magic-based type, and evidence. PE inspection reads headers for architecture, format, subsystem, CLR, execution level, and bounded DLL indicators. Authenticode is queried without executing the file. ZIP formats are listed without extraction. MSI tables and APK binary XML are not parsed in 0.1.

