# Contributing
Use Python 3.10+, install `.[dev]`, and run `pytest`. Detectors must remain read-only, return evidence, survive absent tools, and include fixture tests. Never execute a scanned target.

